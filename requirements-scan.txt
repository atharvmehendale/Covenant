# Python packages needed to run ONE scan pass — nothing more.
#
# The GitHub Actions scheduled run does not open the dashboard, so installing
# Streamlit and the PDF-fixture-building extras on every run would be paying
# for downloads nobody uses. This is the same pinned versions as
# requirements.txt, just the subset a headless scan actually touches, which
# keeps each scheduled run quick.
#
# If you are setting up on your own machine, use requirements.txt instead —
# that one includes the dashboard.

strands-agents==1.53.0
strands-agents-tools==0.8.6
litellm==1.96.0
pypdf==6.14.2
pdfplumber==0.11.10
python-docx==1.2.0
